import React from 'react';

export const ThermalReceiptTemplate: React.FC = () => {
  return (
    <div id="thermalPrintArea" style={{ display: 'none' }}>
      {/* Populated dynamically by PrintAdapter */}
    </div>
  );
};
